// 17) შექმენით class RankedStudent რომელიც Student-სგან იღებს მემკვიდრეობას და ამატებს rank ველს და static sortStudents() მეთოდს

class RankedStudent extends Students {
    constructor(rank){
        this.rank = rank;
    }
    sortStudents(){
        students.sort()
    }
}

